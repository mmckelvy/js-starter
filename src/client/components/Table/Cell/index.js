import React from 'react'

import styles from './styles'

export default function Cell({ cell, isHeader, headerCellStyles, bodyCellStyles, children }) {

}

Cell.propTypes = {
  cell: React.PropTypes.object, // one cell of data (e.g. 'Name' or 'John')

}
