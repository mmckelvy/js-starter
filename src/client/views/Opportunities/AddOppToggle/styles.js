import { color, dims, font, fontSize } from 'client/global-styles'

export default {
  container: {
    color: color.orange900,
    cursor: 'pointer',
    display: 'flex',
    fontFamily: font.secondary,
    fontSize: fontSize.base,
    fontWeight: 300,
    justifyContent: 'center',
    paddingBottom: dims.base * 4,
  },
  icon: {
    marginRight: dims.base * 2,
  },
}
